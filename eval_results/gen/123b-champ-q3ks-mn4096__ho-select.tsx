interface EgovSelectProps {
  id: string;
  name: string;
  title: string;
  options: Array<{ value: string; label: string }>;
  setValue: string;
  setter: (value: string) => void;
}

function EgovSelect({ id, name, title, options, setValue, setter }: EgovSelectProps): JSX.Element {
  console.log("egovSelect", id, name, title, options, setValue, setter);
  return (
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable jsx-a11y/no-onchange */
    // eslint-disable-next-line react/jsx-handler-names
    // Disable the rule because we need to handle onChange event for select element.
    // This is a controlled component and it'll be handled properly.
    /* eslint-disable react/jsx-handler-names */
    // eslint-disable-next-line jsx-a11y/no-onchange
    // Dis