# Python
venv/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.egg

# 모델 weights (수 GB → Git LFS 또는 제외)
models/base/
models/qwen-react-lora-7b*/

# 학습 체크포인트 (중간 저장 파일)
models/*/checkpoint-*/
models/*/optimizer.pt
models/*/rng_state.pth
models/*/scheduler.pt

# 학습 데이터 (생성 가능하므로 제외)
data/processed/

# 환경 설정
.env
*.local

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Jupyter
.ipynb_checkpoints/

# 로그
*.log
runs/
wandb/

# eval 중간 산출물(재생성 가능)
eval_results/
.claude/

# tsc 하드평가 npm 의존성(루트 설치분, 재생성 가능)
node_modules/
package-lock.json

# LAN 파일전송 임시 디렉토리 (대용량 어댑터/생성물 — git에 넣지 않음)
comms/lan_xfer/
