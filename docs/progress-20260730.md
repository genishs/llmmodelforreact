# 진행사항 정리 — 141B/123B 학습·채점 (2026-07-30)

## 1. 141B (Mixtral-8x22B) 학습 — ✅ 완료

**이 gfx1151/64GB 노트북에서 141B 풀학습 완주 실증.**
- 완료 22:48 | Result=success | 3에폭 | 118 step | 총 **11.4h**(평균 344s/step)
- **최종 val_loss 0.6182** (loss 8.69 → 0.62)
- 어댑터 저장: `models/mixtral-8x22b-hqq2-full/adapter_model.safetensors` (139.5MB, 448텐서, 어텐션-온리 0.098%)
- **GTT 피크 47.3GiB / 56 캡**(여유 ~9G), 크래시/OOM/nan 0

### 해결한 두 블로커 (멀티세션 과제)
1. **transformers 5.13 MoE 리팩터링 비호환** — 5.13.1이 Mixtral 전문가를 융합 nn.Parameter로 바꿔 HQQ per-Linear 로더와 충돌. → **격리 venv `ai_model_mixtral`(transformers 4.46.3)** 로 해결(본 `ai_model_rocm` 불변).
2. **rotary 버퍼 device 크래시** — `init_empty_weights`가 non-persistent 버퍼(inv_freq/cos/sin 168개)를 CPU에 남겨 첫 스텝 `indices ... (cpu)` 크래시. → `load_hqq_onthefly` 끝에 버퍼 device 이동(커밋 `0d3fb4a`).

## 2. 123B (Mistral-Large-2411) 재학습 — 🔄 다운로드 중

- 재학습 위해 베이스 재다운로드 중(원본 드라이브 미연결 → 245GB 재다운). 현재 **~39GB/245GB (~16%, 8/51샤드)**, ~10.5MB/s, **ETA ~5.5h**. OS 재시작 후 **resume 확인됨**(7→8샤드, 처음부터 아님).
- ⚠️ 레포가 실은 490GB(HF포맷 245 + 원본 consolidated 245) → **consolidated 제외**로 HF포맷 245GB만.
- OS 재시작 있었으나 부분다운(외장 exfat) 보존 → **resume로 이어받음**(처음부터 아님).
- 완료 후 123B 재학습(본 `ai_model_rocm`, dense라 MoE 이슈 없음), 순차(GPU 하나).

## 3. 채점 트랙 (batch-decode 가속) — 🔄 검증 중

거대모델 채점의 병목(per-token 디코드 ~0.06 tok/s → 123B 20h+ 타임아웃)을 푸는 작업(이슈 #9).
- **가속 코드 구현**: `scripts/gen_batch_utils.py`(길이 버킷팅), `eval_hard_tsc.py --batch-size`(기본값=기존과 바이트 동일, 회귀 0). 16 테스트 통과.
- **채점 관문 통과 확인**:
  - `EGOV_SRC` 해소: `twinspace_platform/sysadmin-front/src` (heldout 7태스크 앵커 전부 존재)
  - GPU 채점 발사 **권한 게이트 없음**(gpu_job.sh 직접 실행)
- **batch=N 실측 배율 측정 중**(141B 로드 ~80분) — 재부팅으로 1회 중단(`bdprobe141bv2`, 100/1624 진행 중 SIGTERM, 웨지 아님) 후 `bdprobe141bv3`로 재발사, 현재 진행 중(23:29 기준 100/1624 양자화 완료, RAM 가용 49.3GB, 정상 궤적). 아직 미확정(정직: probe가 실제 GPU에서 처음 끝까지 도는 건 이번이 처음).
- 123B 채점은 챔피언 어댑터가 **풀 베이스 필요** → 123B 다운로드 완료 후 (adapter_config.json의 base_model_name_or_path=`./models/base/mistral-large-2411` 확인됨).

## 4. 최종 목표 (비교 채점)
141B 어댑터 + 123B fresh 어댑터 + 123B 챔피언 어댑터 → **동일 heldout batch-decode 채점·비교**.

## 5. 인프라/운영
- OS 재시작 handling: 클린 정지(sync) → 재부팅 후 resume 재발사. 스왑 64G 복구.
- GPU 모니터 `strix-halo-monitor`: 전력 3분할 + **디스크 사용율/부족경고(v0.3.0)** + **네트워크 속도(v0.4.0, 개발중)**. 전부 statvfs/커널카운터-only(학습 무간섭).
- 형상: 로컬 커밋 다수(0d3fb4a·667be1e·e9b6b9e 등), **push는 두목 지시 대기**.

---
*halo 장비 · 완전자율 운영 · halo-pm 조율. 채점 실측 배율·123B 결과는 후속 기록.*
